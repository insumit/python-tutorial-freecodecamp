# A tuple is a container storing different values
coordinates = (4, 5)
print(coordinates[0])
# Difference between tuple and lists
# Lists are mutable and tuple is not mutable i.e cannot be modified whatsoever
